
/**
 * Checks if there's a user logged in.
 */
export const isLogged = () => {
  // Check if user is logged, like checking for token in localStorage
  return false
}

/**
 * Do login logic
 */
export const doLogin = () => {
  console.warn('doLogin is called but was not implemented.')
}
